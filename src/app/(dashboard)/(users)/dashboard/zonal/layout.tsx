import ZonalDahboardLayout from "@/layouts/dashboard/zonal";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <ZonalDahboardLayout>{children}</ZonalDahboardLayout>;
}
