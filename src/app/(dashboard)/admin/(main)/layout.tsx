import AdminDahboardLayout from "@/layouts/dashboard/admin";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <AdminDahboardLayout>{children}</AdminDahboardLayout>;
}
